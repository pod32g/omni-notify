# Binaries
/omni-notify
/bin/
*.exe

# Local database files
*.db
*.db-wal
*.db-shm

# Local config with real secrets
/config.yaml

# Local docker / deploy artifacts (data lives in a named volume; backups on host)
/data/
/backups/
/.env

# Test/coverage artifacts
*.out
coverage.*

# Editor/OS
.DS_Store
.claude/
